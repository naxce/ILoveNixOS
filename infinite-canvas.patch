diff --git a/Config/hypr/canvas.lua b/Config/hypr/canvas.lua
new file mode 100644
index 0000000..d604bed
--- /dev/null
+++ b/Config/hypr/canvas.lua
@@ -0,0 +1,95 @@
+-- Infinite 2D workspace canvas.
+--
+-- Workspaces named "canvas_<x>_<y>" form a boundless grid you can pan across
+-- in any of the 4 directions, forever. Stepping onto a cell that doesn't
+-- exist yet creates it on the fly, exactly like any other named Hyprland
+-- workspace; stepping away from an empty one lets Hyprland clean it back up
+-- automatically. Nothing here reads or writes any state file - the current
+-- cell is always derived live from whichever workspace is actually focused,
+-- so it can never drift out of sync.
+--
+-- This is entirely additive: it lives alongside the normal 1-10 workspaces
+-- in monitors.lua/binds.lua and never touches them.
+
+local mainMod   = "SUPER"
+local canvasMod = mainMod .. " + ALT"
+
+local CANVAS_PREFIX = "canvas_"
+
+-- Reads the currently focused workspace and returns its (x, y) canvas
+-- coordinate. Falls back to the origin (0, 0) when the active workspace
+-- isn't a canvas cell at all - e.g. you're on workspace "1" - so every
+-- canvas bind is always safe to press from anywhere.
+local function current_coords()
+    local ws = hl.get_active_workspace()
+    if ws and ws.name then
+        local x, y = ws.name:match("^" .. CANVAS_PREFIX .. "(%-?%d+)_(%-?%d+)$")
+        if x then
+            return tonumber(x), tonumber(y)
+        end
+    end
+    return 0, 0
+end
+
+local function cell_name(x, y)
+    return CANVAS_PREFIX .. x .. "_" .. y
+end
+
+-- Small on-screen toast so panning isn't flying blind. Wrapped in pcall so
+-- a notification-API mismatch never breaks the actual pan/move above it.
+local function notify(x, y)
+    pcall(function()
+        hl.notification.create({
+            text    = "Canvas   " .. x .. ", " .. y,
+            timeout = 900,
+        })
+    end)
+end
+
+-- Pan focus only; the window you're looking at stays where it is.
+local function pan(dx, dy)
+    return function()
+        local x, y   = current_coords()
+        local nx, ny = x + dx, y + dy
+        hl.dispatch(hl.dsp.focus({ workspace = cell_name(nx, ny) }))
+        notify(nx, ny)
+    end
+end
+
+-- Pan and bring the active window along for the ride.
+local function pan_with_window(dx, dy)
+    return function()
+        local x, y   = current_coords()
+        local nx, ny = x + dx, y + dy
+        hl.dispatch(hl.dsp.window.move({ workspace = cell_name(nx, ny), follow = true }))
+        notify(nx, ny)
+    end
+end
+
+local function go_home()
+    hl.dispatch(hl.dsp.focus({ workspace = cell_name(0, 0) }))
+    notify(0, 0)
+end
+
+local directions = {
+    left  = { dx = -1, dy = 0  },
+    right = { dx = 1,  dy = 0  },
+    up    = { dx = 0,  dy = -1 },
+    down  = { dx = 0,  dy = 1  },
+}
+
+for key, d in pairs(directions) do
+    hl.bind(canvasMod .. " + " .. key, pan(d.dx, d.dy))
+    hl.bind(canvasMod .. " + SHIFT + " .. key, pan_with_window(d.dx, d.dy))
+end
+
+hl.bind(canvasMod .. " + C", go_home)
+
+-- To send a window off to an adjacent cell WITHOUT following it there,
+-- add a third tier the same way binds.lua does for resize, e.g.:
+--
+-- hl.bind(canvasMod .. " + CTRL + " .. key, function()
+--     local x, y   = current_coords()
+--     local nx, ny = x + d.dx, y + d.dy
+--     hl.dispatch(hl.dsp.window.move({ workspace = cell_name(nx, ny), follow = false }))
+-- end)
diff --git a/Config/hypr/hyprland.lua b/Config/hypr/hyprland.lua
index 69461f0..aa693eb 100644
--- a/Config/hypr/hyprland.lua
+++ b/Config/hypr/hyprland.lua
@@ -2,6 +2,7 @@ require("monitors")
 require("input")
 require("looknfeel")
 require("binds")
+require("canvas")
 require("windowrules")
 require("autostart")
 
diff --git a/Config/waybar/config.jsonc b/Config/waybar/config.jsonc
index c2bfe82..21c0c4f 100644
--- a/Config/waybar/config.jsonc
+++ b/Config/waybar/config.jsonc
@@ -7,7 +7,7 @@
     "margin-right": 10,
     "spacing": 0,
 
-    "modules-left": ["hyprland/workspaces", "custom/minimized", "hyprland/window"],
+    "modules-left": ["hyprland/workspaces", "custom/minimized", "custom/canvas", "hyprland/window"],
     "modules-center": ["clock"],
     "modules-right": ["tray", "network", "bluetooth", "pulseaudio", "custom/notification", "custom/power"],
 
@@ -16,7 +16,7 @@
         "on-click": "activate",
         "all-outputs": false,
         "active-only": false,
-        "ignore-workspaces": ["special:minimized"],
+        "ignore-workspaces": ["special:minimized", "^canvas_"],
         "format-icons": {
             "active": "●",
             "default": "○",
@@ -35,6 +35,13 @@
         "on-click": "hyprctl dispatch togglespecialworkspace minimized"
     },
 
+    "custom/canvas": {
+        "exec": "$HOME/NixOS/Scripts/canvas-status.sh",
+        "return-type": "json",
+        "interval": 1,
+        "on-click": "hyprctl dispatch \"hl.dsp.focus({ workspace = 'canvas_0_0' })\""
+    },
+
     "hyprland/window": {
         "format": "{title}",
         "max-length": 40,
diff --git a/Config/waybar/style-dachshund.css b/Config/waybar/style-dachshund.css
index e59009a..01eedac 100644
--- a/Config/waybar/style-dachshund.css
+++ b/Config/waybar/style-dachshund.css
@@ -59,6 +59,23 @@ window#waybar {
     border-radius: 8px;
 }
 
+#custom-canvas {
+    color: #c48a5c;
+    padding: 0 10px;
+    margin-left: 2px;
+}
+
+#custom-canvas.empty {
+    padding: 0;
+    margin: 0;
+}
+
+#custom-canvas.on-canvas:hover {
+    color: #f0dcc0;
+    background: rgba(168, 92, 50, 0.16);
+    border-radius: 8px;
+}
+
 #clock,
 #tray,
 #network,
diff --git a/Config/waybar/style.css b/Config/waybar/style.css
index 951083f..c902d01 100644
--- a/Config/waybar/style.css
+++ b/Config/waybar/style.css
@@ -59,6 +59,23 @@ window#waybar {
     border-radius: 8px;
 }
 
+#custom-canvas {
+    color: #b0b0b0;
+    padding: 0 10px;
+    margin-left: 2px;
+}
+
+#custom-canvas.empty {
+    padding: 0;
+    margin: 0;
+}
+
+#custom-canvas.on-canvas:hover {
+    color: #ffffff;
+    background: rgba(255, 255, 255, 0.08);
+    border-radius: 8px;
+}
+
 #clock,
 #tray,
 #network,
diff --git a/Scripts/canvas-status.sh b/Scripts/canvas-status.sh
new file mode 100755
index 0000000..da1fc21
--- /dev/null
+++ b/Scripts/canvas-status.sh
@@ -0,0 +1,11 @@
+#!/usr/bin/env bash
+
+name=$(hyprctl activeworkspace -j | jq -r '.name')
+
+if [[ "$name" =~ ^canvas_(-?[0-9]+)_(-?[0-9]+)$ ]]; then
+    x="${BASH_REMATCH[1]}"
+    y="${BASH_REMATCH[2]}"
+    echo "{\"text\": \"${x}, ${y}\", \"tooltip\": \"On the infinite canvas at ${x}, ${y} - click to go home\", \"class\": \"on-canvas\"}"
+else
+    echo '{"text": "", "tooltip": "Not on the infinite canvas", "class": "empty"}'
+fi
diff --git a/Scripts/hotkeys.sh b/Scripts/hotkeys.sh
index 5989004..efb8063 100644
--- a/Scripts/hotkeys.sh
+++ b/Scripts/hotkeys.sh
@@ -45,6 +45,10 @@ Super + 1..0	Go to workspace 1-10
 Super + Shift + 1..0	Move active window to workspace 1-10
 Super + Ctrl + Left/Right	Go to previous/next workspace
 Super + Mouse wheel	Scroll through workspaces
+── Infinite canvas ──	
+Super + Alt + Arrows	Pan across the infinite 2D canvas
+Super + Alt + Shift + Arrows	Pan the canvas, bringing the active window with you
+Super + Alt + C	Jump straight back to canvas home (0, 0)
 ── Screenshots & tools ──	
 Print	Screenshot a selected region, opens in Swappy to edit
 Shift + Print	Screenshot the full screen, opens in Swappy to edit
@@ -77,6 +81,7 @@ waybar_binds() {
 ── Workspaces & window ──	
 Click workspace	Jump to that workspace
 Click minimized indicator	Show/hide the minimized workspace
+Click canvas indicator	Jump straight back to canvas home (0, 0)
 Click clock/window module	Display only, no action
 ── System tray ──	
 Click network icon	Open nm-connection-editor
diff --git a/home.nix b/home.nix
index 0e69b2d..9813eda 100644
--- a/home.nix
+++ b/home.nix
@@ -127,6 +127,7 @@
   home.file.".config/nvim".source = ./Config/nvim;
   home.file.".config/hypr/autostart.lua".source = ./Config/hypr/autostart.lua;
   home.file.".config/hypr/binds.lua".source = ./Config/hypr/binds.lua;
+  home.file.".config/hypr/canvas.lua".source = ./Config/hypr/canvas.lua;
   home.file.".config/hypr/hyprland.lua".source = ./Config/hypr/hyprland.lua;
   home.file.".config/hypr/input.lua".source = ./Config/hypr/input.lua;
   home.file.".config/hypr/looknfeel.lua".source = ./Config/hypr/looknfeel.lua;
